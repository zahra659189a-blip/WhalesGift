# 🔐 متغيرات البيئة المطلوبة على Render

## ⚠️ مهم جداً
يجب إضافة جميع هذه المتغيرات في **Environment Variables** على Render

---

## 🤖 إعدادات البوت الأساسية

### BOT_TOKEN (مطلوب)
```
توكن البوت من @BotFather
مثال: 7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
```

### BOT_USERNAME (مطلوب)
```
اسم مستخدم البوت بدون @
مثال: Arab_ton_bot
```

### ADMIN_IDS (مطلوب)
```
معرفات التيليجرام للأدمن مفصولة بفواصل
مثال: 1797127532,6603009212
```

---

## 🌐 إعدادات الروابط

### MINI_APP_URL (مطلوب)
```
رابط Mini App على Vercel
القيمة: https://arabton.vercel.app
```

### FRONTEND_URL (مطلوب)
```
نفس رابط Mini App
القيمة: https://arabton.vercel.app
```

### API_BASE_URL (مطلوب)
```
رابط API على Render
القيمة: https://arabton.onrender.com/api
```

---

## 💾 إعدادات قاعدة البيانات

### DATABASE_PATH (مطلوب)
```
مسار قاعدة البيانات على Render
القيمة: /opt/render/project/src/Arab_ton.db
```

### DATABASE_URL (اختياري للـ PostgreSQL)
```
رابط PostgreSQL إذا كنت تستخدمه بدلاً من SQLite
```

---

## 🔐 إعدادات الأمان

### ADMIN_USERNAME (مطلوب)
```
اسم المستخدم لتسجيل الدخول للوحة الأدمن
مثال: OmarShehata@123
```

### ADMIN_PASSWORD_HASH (مطلوب)
```
هاش كلمة المرور للأدمن (SHA256)
لتوليد الهاش: python generate_admin_password.py
```

### JWT_SECRET (مطلوب)
```
مفتاح سري لتوليد JWT tokens
يمكن توليده تلقائياً أو استخدام: openssl rand -hex 32
```

### SECRET_KEY (مطلوب)
```
مفتاح سري للبوت
يمكن توليده تلقائياً أو استخدام: openssl rand -hex 32
```

---

## 💰 إعدادات TON (للسحوبات - اختياري)

### TON_WALLET_ADDRESS (اختياري)
```
عنوان محفظة TON الخاصة بك
```

### WALLET_MNEMONIC (اختياري)
```
كلمات المحفظة (24 كلمة مفصولة بمسافات)
⚠️ احتفظ بها سرية جداً!
```

### TON_API_KEY (اختياري)
```
مفتاح API من TON Center
```

---

## 📢 إعدادات القنوات (اختياري)

### PAYMENT_PROOF_CHANNEL (اختياري)
```
معرف قناة إثباتات الدفع
مثال: @Arab_ton_proofs
أو معرف القناة: -1001234567890
```

---

## ⚙️ إعدادات النظام

### PYTHON_VERSION
```
القيمة: 3.12.7
(موجودة بالفعل في render.yaml)
```

### PORT
```
يتم تعيينها تلقائياً بواسطة Render
```

### RENDER
```
يتم تعيينها تلقائياً بواسطة Render
```

---

## 📝 ملاحظات مهمة

1. **لا تشارك المتغيرات السرية** (BOT_TOKEN, WALLET_MNEMONIC, etc.)
2. **احتفظ بنسخة احتياطية** من جميع المتغيرات في مكان آمن
3. **تأكد من صحة جميع الروابط** قبل النشر
4. **غيّر كلمات المرور الافتراضية** فوراً

---

## 🔄 كيفية إضافة المتغيرات على Render

1. اذهب إلى Dashboard > Your Service
2. اضغط على **Environment** في القائمة الجانبية
3. اضغط على **Add Environment Variable**
4. أضف كل متغير مع قيمته
5. اضغط **Save Changes**
6. أعد نشر الخدمة (Redeploy)

---

## ✅ التحقق من المتغيرات

بعد إضافة المتغيرات، تحقق من الـ Logs:
- يجب أن ترى: `✅ Bot process started`
- يجب أن ترى: `🚀 Bot thread started on Render`
- يجب ألا ترى: `⚠️ WARNING: Using default admin password!`

---

## 🆘 حل المشاكل

### البوت لا يعمل؟
- تحقق من BOT_TOKEN
- تحقق من ADMIN_IDS
- تحقق من المتغيرات في Environment Variables

### خطأ Python 3.14؟
- احذف الخدمة القديمة من Render
- أنشئ خدمة جديدة
- تأكد من PYTHON_VERSION = 3.12.7

### البوت لا يرد على /start؟
- تحقق من أن البوت يعمل في الـ Logs
- تحقق من BOT_USERNAME
- تحقق من MINI_APP_URL
